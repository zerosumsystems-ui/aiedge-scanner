"""Signal layer — setup detection. THE BRAIN.

`bpa_detector.py` is the canonical Brooks setup detector. It is the
PRIMARY input to signal determination — not a post-hoc decoration.

Both live scanner and backtest import from here. That is the invariant
that guarantees research == production.
"""
