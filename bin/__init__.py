"""CLI entry points.

`brooks_score_cli` is the synthetic-data demo + historical-universe
scan CLI. The live scanner is at `aiedge.runners.live:main` (and
`python live_scanner.py` still works as a shim).
"""
